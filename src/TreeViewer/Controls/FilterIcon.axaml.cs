using Avalonia;
using Avalonia.Controls;
using Avalonia.Markup.Xaml;

namespace TreeViewer
{
    public class FilterIcon : UserControl
    {
        public FilterIcon()
        {
            InitializeComponent();
        }

        private void InitializeComponent()
        {
            AvaloniaXamlLoader.Load(this);
        }
    }
}
